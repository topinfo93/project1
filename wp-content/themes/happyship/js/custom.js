;(function($) {

   'use strict'

        	   //  $('.menu_right li').click(function () {			
            //         $(this).find('.menu_hover').toggle();
            //     });
        	   //  $('.menu_right li').hover(function () {
            //         //$(this).find('.menu_hover').attr("style","top: 35px");
            //         $(this).find('.menu_hover').toggle();
            //     });

            // $(document).mouseup(function (e) {
            //     var container = $(".menu_hover,.menu_hover");
            //     if (!container.is(e.target) // if the target of the click isn't the container...
            //         && container.has(e.target).length === 0) // ... nor a descendant of the container
            //     {
            //         $('.menu_hover').hide();
            //     }
            // });


        // $("select[name='kh_quan']").on('change', function() {
            
        //     var kh_quan = $("select[name='kh_quan']").val(),
        //         kh_goi = $("select[name='kh_goi']").val();

        //     if (kh_quan != null && kh_goi != null) {
        //         var data = {
        //             'action': 'sb_test_ajax',
        //             'kh_quan': kh_quan,
        //             'kh_goi': kh_goi
        //         };

        //         var ajaxurl = '/wp-admin/admin-AJAX.php';


        //         $.post(ajaxurl, data, function(response){
        //             console.log(response);

        //             $('.fee .right').empty().append(response+'đ');
        //             $('.fee input[name="fee"]').val(response);

        //         });
        //     }

        // });


})(jQuery);